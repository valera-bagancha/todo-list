export const registrationHandler = (event: Event) => {
  event.preventDefault();
  console.log('registration');
  
  //const isValidEmail = emailTest();
  // const isValidPassword = passwordTest();
  // const isValidConfirmPassword = repeatPasswordTest();
  // if (isValidEmail && isValidPassword && isValidConfirmPassword) {
  //     renderMainPage()
  // }
}